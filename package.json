{
  "name": "wallverse-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --port 8788",
    "check:functions": "wrangler pages functions build --outdir .wrangler/seo-build",
    "test:seo": "node tests/seo-smoke.mjs"
  },
  "devDependencies": { "wrangler": "4.131.0" }
}
